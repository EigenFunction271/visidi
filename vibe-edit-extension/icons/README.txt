Icons referenced in manifest.json (icon16.png, icon48.png, icon128.png) are
not included in this scaffold — they need real PNG assets before the
extension will load cleanly (Chrome will warn but may still load without
them in dev). Any placeholder square icon works for the workshop; this is
not a functional blocker, just a missing asset.
